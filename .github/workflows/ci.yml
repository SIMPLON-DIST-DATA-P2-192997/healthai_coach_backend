name: CI

on:
  pull_request:
    branches: [main, dev]
  push:
    branches: [main, dev]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # Squelette Sprint 0 — logique de lint/tests réelle ajoutée
      # au fil des sprints (ETL, API, data quality)
      - name: Lint (placeholder)
        run: echo "TODO: lint (ruff/flake8)"

      - name: Tests (placeholder)
        run: echo "TODO: pytest"
